[ids]
*

[main]
# Disables standard Caps Lock toggling and redefines it as a layer modifier
capslock = layer(nav_layer)

[nav_layer]
# Maps Vim-style keys to arrow keys when Caps Lock is held down
h = left
j = down
k = up
l = right
